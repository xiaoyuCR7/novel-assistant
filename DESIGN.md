# 小说创作室 · 视觉规范

以 macOS 原生创作工具为参考：安静、清晰、耐看，让小说内容占据中心。

- 颜色：正文 `#1d1d1f`，辅助文字 `#6e6e73`，白色内容面 `#ffffff`，银灰背景 `#f5f5f7`，操作蓝 `#0071e3`。蓝色用于按钮、链接与选中状态。小字使用白底蓝字或蓝底白字，避免蓝字直接放在浅灰底上。错误与警告保留语义颜色。
- 字体：系统无衬线字体负责界面，中文优先苹方及微软雅黑回退；宋体/系统衬线字体用于小说正文。界面正文 14–15px，资料与表单辅助信息至少 12px，标题 20–32px。
- 布局：固定导航与工具栏、独立滚动内容、底部输入框。模型状态并入工具栏，创作准备呈现为紧凑提示。侧栏可收成图标导航；收起后从顶部打开章节目录。正文分栏优先分配 58% 宽度。欢迎区与输入框对齐，空对话不自动滚到底部。
- 控件：8–12px 圆角，输入区与弹窗 16–20px。以 4px 为基础的间距令牌组织对齐；资料库使用分隔列表。阴影仅用于弹窗或浮起的选中控件，弹窗遮罩不使用模糊。统一悬停、焦点、禁用、加载光标与错误边框。
- 小屏：侧栏转为底部三个主要导航入口，章节与资料从顶部访问，输入区可见；低高度视口允许整个工作区滚动。
- 可访问性：可见键盘焦点、可读辅助文字、触控目标至少 44px；支持减少动态效果与增强对比度。保留所有草稿、确认、费用和失败状态。
- 页面切换：工作区、章节和回收站切换采用 180ms 淡入与 4px 位移；首次打开、输入与后台刷新不重复播放。动画附着现有容器，不通过重建组件实现；连续切换取消旧动画，系统减少动态效果偏好开启时跳过或立即取消。

共享颜色在 `frontend/src/styles/tokens.css`；当前创作工作台与共享界面收口在 `frontend/src/styles/focus.css`。
